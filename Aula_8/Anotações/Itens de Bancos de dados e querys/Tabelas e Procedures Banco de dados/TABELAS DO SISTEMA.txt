CREATE TABLE TBCLIENTES (
ID_CLI INT PRIMARY KEY IDENTITY,
NOME_CLI VARCHAR(50))

GO

CREATE TABLE TBPRODUTOS(
ID_PROD INT PRIMARY KEY IDENTITY,
NOME_PROD VARCHAR(50),
QTD_PROD  INT,
VL_PROD  MONEY)

GO

CREATE TABLE TBITENSVENDA (
ID_Item_venda INT PRIMARY KEY IDENTITY,
ID_Cod_Venda  INT ,
ID_Prod       INT,
Qtd_Prod      INT,
VL_Prod       MONEY )
GO

CREATE TABLE TBVENDAS (
ID_Venda      INT PRIMARY KEY IDENTITY,
ID_Cod_Venda  INT,
ID_Cli        INT,
Total         MONEY, 
DT_Venda      DATETIME)

ALTER TABLE TBITENSVENDA
ADD CONSTRAINT fk_ItensProd FOREIGN KEY (ID_Prod) REFERENCES TBPRODUTOS (ID_Prod)

GO
ALTER TABLE TBVENDAS
ADD CONSTRAINT fk_VenCli FOREIGN KEY (ID_Cli) REFERENCES TBCLIENTES (ID_Cli)


